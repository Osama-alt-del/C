/* I will try and make a cube using ACII in C 

Or better yet, lets make a graphics engine. Every to the camera that an object is, the 'thicker' the ascii character
will be. (or actually this depends on the light that hits the object.) We will assume that the light and the
camera are in the same position for now*/

#include <stdio.h>

int main()
{

return 0;
}

