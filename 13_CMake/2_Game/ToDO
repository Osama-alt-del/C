ToDo:

 - make it so that the level and the gamestate values are changed within the stack frames (functions that are called) (call by reference)
 - have enums in type.h for all of the pages (settings and stuff) and make it so that we can check selection using those.



 - Save the current level and all of the data in a .json file.
