// This is a program where I will take an input from the terminal directly and then reverse and try to output the string

#include <stdio.h>

void reverse(char* string);

// so argv is a double array of chracters, meaning it can hold strings, (character arrays) inside of it)
int main(int argc, char *argv[]){
    printf("%s\n", argv[1]);
    printf("%s\n", reversed);
    return 0;
}


void reversed(char* string){
    // Find the string size, and then reverse the string
    
}

