ToDo:

 - Make it sot hat there is a "Game" struct which will track the current level and the current state of the game
 - Based on the values held by that game struct, we will know what to do.
 - have a switch statement in the main game loop (game() in game.c)
 - amke it so that the level and the gamestate values are changed within the stack frames (functions that are called) (call by reference)
